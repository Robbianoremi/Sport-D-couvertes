{# templates/reservation/new.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
<div class="container d-flex justify-content-center align-items-center vh-100">
    
    <div class="row">
        <div class="col-lg-12 mx-auto border border-2 shadow rounded p-4">

            <h3 class="text-center mb-5">Nouvelle réservation</h3>
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-lg btn-primary mt-2">Réserver</button>
            {{ form_end(form) }}
        </div>
    </div>
   
</div>




{% endblock %}